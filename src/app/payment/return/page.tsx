export default function PaymentReturnPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[#0A0A0A] px-6 text-white">
      <div className="max-w-lg text-center">
        <p className="font-mono text-sm uppercase tracking-[0.35em] text-neon-green">Payment Settings</p>
        <h1 className="mt-4 font-heading text-4xl font-black">RETURNING TO SCROLLTOLL</h1>
        <p className="mt-4 text-gray-400">If the app does not open automatically, return to ScrollToll manually.</p>
      </div>
    </main>
  );
}
